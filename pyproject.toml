[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "napari-zf-microglia-ai"
version = "0.2.0"
description = "ZF-Microglia-AI napari plugin — zebrafish brain extraction, microglia segmentation (Pixel Classifier / Cellpose-SAM), and AI-assisted statistics"
requires-python = ">=3.8"
dependencies = ["napari", "monai", "tifffile", "numpy", "qtpy", "superqt", "cellpose"]

[project.scripts]
zf-microglia-ai = "napari_zf_microglia_ai.__main__:main"

[project.entry-points."napari.manifest"]
napari-zf-microglia-ai = "napari_zf_microglia_ai:napari.yaml"

[tool.setuptools.packages.find]
where = ["."]
include = ["napari_zf_microglia_ai*"]

[tool.setuptools.package-data]
"napari_zf_microglia_ai" = ["napari.yaml"]
